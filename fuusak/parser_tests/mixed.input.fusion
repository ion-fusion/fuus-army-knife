// Add 1 and 2
(+ 1 2)

// Define a function
(define (some_fun a b)
  (+ 1 2))

(define (trueFunc a)
  (+ 1 a))

(define (
         some_fun // Comment on function
         a // Comment on arg a
         b /* Comment on arg b */
         )
  (+ 1 2))

(define SOME_STRUCT {
   a: "a",
   b: '''b''',
   c: {
      'foo bar': (+ 3 4),
      baz: 3,
      biz: [1, 2],
      ts: 2020-11-20T15:49:00.100Z,
   }
})
