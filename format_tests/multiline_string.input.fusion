'''foo bar'''

'''foo
'''

'''

foo
  bar
    baz

'''


(define foo '''
foo
''')

(define foo '''
            foo
              bar
                biz
            baz
            ''')

(define (foo a b)
         '''
         some description of foo

         with an example:

           (foo 1 2)
         '''
  (some code))

(define (foo a b)
         '''
some description of foo

with an example:

  (foo 1 2)
         '''
  (some code))
